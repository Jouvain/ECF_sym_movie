{% extends 'base.html.twig' %}

{% block title %}Mon compte SYM-Movies{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if app.user is defined and app.user != null %}
    <h1>Bonjour {{ app.user.prenom }} !</h1>

    Informations personnelles :
    <ul>
        <li> Nom : {{app.user.nom}} </li>
        <li> Prenom : {{app.user.prenom}}  </li>
        <li> Mail : {{app.user.email}} </li>
    </ul>

    <a href="{{ path('app_compte_delete', {id: app.user.id }) }}">
        Se désabonner
    </a>
    <a href="{{ path('app_compte_edit', {id: app.user.id }) }}">
        Modifier ses informations
    </a>

    {% else %}
    <h1> Vous ne devriez pas voir cette page ! </h1>
    {% endif %}

</div>
{% endblock %}
